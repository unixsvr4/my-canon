
  {{ inventory_hostname }} - managed by Ansible (baseline role)
  Owner:   {{ baseline_motd_owner }}
  Contact: {{ baseline_motd_contact }}

  Local changes are reverted automatically and reported as drift.

